# 当前进展（2026-05-27 快照）

> 本文档基于 `docs/里程碑规划.md` + `docs/iterations/` 下各 spec，对照 `crates/`、`src-tauri/`、`src/` 的现状逐条核对得到。每次有较大节点完成时，建议覆盖更新本文件。

## TL;DR

- **MS0 工程基座** —— 完成
- **MS1 主体库 + 账号管理** —— 完成（含 spec-14 全部 UI 落地，spec-13 软删除增强）
- **MS2 AI 生图/生视频** —— 主体功能完成（spec-15 ~ spec-19 全部落地，并已叠加诊断事件流、参考图修复、账号软删除、ModelPicker 多账号选择等增强）；剩真机 smoke 验证 happyhorse 完整链路
- **MS3 节点画布** —— 当前分支 `feature/20260527-flow-1`，尚未开始实施
- **MS4** —— spec-25/26/27 已落地（见下），剩 spec-28~31；**MS5/MS6** —— 未启动

测试规模：Rust workspace 270+ 通过（含 retry/batch 集成测试）；前端 36 文件 / 150 测试通过。
代码规模：核心代码合计约 22.6k 行（core 66 文件、frontend 105 文件）。

---

## MS0：工程基座

| spec | 主题 | 状态 | 落点 |
|---|---|---|---|
| spec-01 | Cargo workspace（core + cli + src-tauri） | ✅ | `Cargo.toml`, `crates/` |
| spec-02 | Tauri 2 + React 19 + Vite + pnpm | ✅ | `package.json`, `src-tauri/` |
| spec-03 | SQLite 全量 schema + 迁移框架 | ✅ | `crates/core/src/db/migrations/`（已演进到 008） |
| spec-04 | Project CRUD | ✅ | `crates/core/src/db/queries/project.rs` |
| spec-05 | Tauri IPC（project commands） | ✅ | `src-tauri/src/commands/project.rs` |
| spec-06 | CLI 骨架 | ✅ | `crates/cli/src/commands/project.rs` |
| spec-07 | 类型生成（specta 单源） | ✅ | `src/lib/bindings/` |
| spec-08 | App Shell UI | ✅ | `src/components/layout/`, `src/routes.tsx` |
| spec-09 | 项目页面 | ✅ | `src/pages/ProjectListPage.tsx`, `ProjectWorkspacePage.tsx` |

**关键决策档案**：specta 取代手写 ts-rs 通道作为 IPC 类型单一来源（spec-07 已重写）。

---

## MS1：主体库 + 账号管理

| spec | 主题 | 状态 | 落点 |
|---|---|---|---|
| spec-10 | 每项目数据目录 | ✅ | `crates/core/src/paths.rs`, `migrations/002_add_project_root_path.sql` |
| spec-11 | character/scene/prop/costume CRUD | ✅ | `crates/core/src/db/queries/{character,scene,prop,costume}.rs` + `src-tauri/src/commands/` 对应文件 |
| spec-12 | 素材导入管线（拖入→assets/→缩略图→入库） | ✅ | `crates/core/src/asset/{import,thumbnail}.rs` |
| spec-13 | 账号 keyring 加密存储 | ✅ + 增强 | `crates/core/src/account/` + `migrations/003_add_api_account_key_last4.sql` |
| spec-14 | 主体库 / 主体详情 / 账号管理页面 | ✅ | `src/pages/SubjectLibraryPage.tsx`, `SubjectDetailPage.tsx`；`src/components/{subjects,accounts,settings}/` |

**spec-13 已超原 spec 落地的增强**：
- migration 007 给 `api_account` 增加 `deleted_at` 列，service 层从 `DELETE` 改为 tombstone（保留历史任务的 FK 引用同时把 keyring 凭证擦除）—— 2026-05-27
- session-scoped keyring 缓存避免重复弹出 Keychain 授权（commit 85ee750）

**spec-12 已超原 spec 落地的增强**：
- migration 008 给 `asset` 表加 `source` / `label` 列，配 `AssetLibraryPage` + `AssetFilterBar` 支持按来源 / 标签过滤（commit be9571b）

---

## MS2：AI 生图/生视频

> 总体已实现端到端链路：建账号 → 建主体 → 提交任务 → 实时事件 → 结果入库为 Asset。剩余事项集中在「真机 smoke + 边角细节」。

### 文档与决策

- `docs/iterations/ms2-AI生图生视频/` 下五个 spec（15 ~ 19）+ `dependencies.md` 全部落库
- **首批模型决策**：从原计划的「可灵」改为百炼双模型（`wan2.7-image-pro` 文生图 + `happyhorse-1.0-r2v` 参考图生视频）—— 2026-05-25 已同步至 `里程碑规划.md` / `项目目标.md` / `技术选型.md`
- **参考图 URL 化决策**：阿里 OSS，用户自带 bucket，独立 `asset_uploader` 模块 —— 2026-05-25

### 实施进度

| spec | 主题 | 状态 | 落点 |
|---|---|---|---|
| spec-15 | 任务引擎骨架（ModelProvider trait / TaskEngineHandle / events） | ✅ | `crates/core/src/provider/{traits,registry,stub}.rs`, `crates/core/src/task_engine/`（events/handle/runner/state/materializer） |
| spec-16 | OSS uploader + `api_account.params_json` | ✅ | `crates/core/src/provider/asset_uploader/{traits,oss}.rs`；`migrations/004_add_api_account_params_json.sql`；前端 `OssFields.tsx` |
| spec-17 | BailianProvider（双模型 + ResultMaterializer） | ✅ | `crates/core/src/provider/bailian/{client,wan27,happyhorse,materializer,types}.rs`；`crates/core/src/asset/from_remote.rs` |
| spec-18 | 提交表单 + 任务列表 + 结果预览 UI | ✅ | `src/components/generation/`（ModelPicker / SubmitTaskPanel / TaskListPanel / TaskCard / ResultPreviewDialog / params/） |
| spec-19 | CLI `task submit/status/list/cancel` | ✅ | `crates/cli/src/commands/task/`（含 `--wait` 实时事件、`--json` NDJSON 输出） |

### 后续叠加的增强（已合入）

| 主题 | 状态 | 落点 |
|---|---|---|
| `generation_task.project_id` 字段补齐 + 清理流程改进 | ✅ | migration 005，commit 482118a |
| **诊断事件流**：`generation_task_event` 表 + `ProviderErrorDetail` + `TaskEvent::{EventLogged,ProgressTick}` + `TaskDiagnosticsDialog` + CLI `--verbose/--wait` 实时事件 | ✅ | migration 006，commit 2981471 |
| **OSS 错误诊断真相还原**：`aliyun-oss-client` `Display` 硬编码 "oss error" 的问题用 `oss_error_detail` helper 通过 `{:?}` + variant 分类还原 | ✅ | `provider/asset_uploader/oss.rs`，2026-05-26 |
| **happyhorse 卡 pending 修复**：①`is_valid_transition` 补 `(Pending, Failed)` 边；②参考图改走 `resolveCharacterReferenceAssets`（按 `character.reference_image_path` 反查 asset_id） | ✅ | `task_engine/state.rs`、`SubmitTaskPanel.tsx`，2026-05-26 |
| **ModelPicker 多账号显式选择**：同一 provider 下 ≥2 账号时显示「使用账号」二级 Select，stale accountId 自动重锚 | ✅ | `src/components/generation/ModelPicker.tsx`，2026-05-27 |
| **账号软删除**：避免 happyhorse 完成后再删账号触发 `FOREIGN KEY constraint failed` | ✅ | migration 007，2026-05-27 |

### 真机验证 backlog（来自 worklog）

源于 2026-05-26 17:25 段：
- (a) 故意 OSS endpoint 错误的 happyhorse → 时间线含 `submit_upload error`
- (b) 真实 wan2.7 → running 显示「提交到云端」→ 终态「已存入素材库」
- (c) `mango task submit ... --wait` 看 stderr 实时事件
- (d) `mango task status <id> --verbose --json` 检查 events 数组

源于 2026-05-27 11:05 段：
- 复测：删除曾经报 FK 失败的账号现在应成功；该账号下的任务 + 诊断事件仍可访问；新建任务时 ModelPicker 不再列出该账号

源于 2026-05-26 18:10 段：
- happyhorse 复测真实失败时 `TaskDiagnosticsDialog` 应能显示具体 OSS 错误（`InvalidAccessKeyId` / `SignatureDoesNotMatch` / `Reqwest(...)` 等）

### 已识别但未做的小尾巴（不阻塞）

- `account::service` 中 `_existing` / `_exists` 命名整理（`service.rs:161/264`）
- `character` 表加 `reference_asset_id` 列消掉「按 file_path 反查 asset」的间接性
- runner 在 task 终态时回写账号 `usage_used` / `last_used_at`（目前未打通；这是 MS4 用量统计的前置）
- 「设置默认账号」字段，避免多账号场景每次提交都要人工选

---

## MS3：节点画布

- 当前分支：`feature/20260527-flow-1`
- `docs/iterations/ms3-节点画布/` 下仅有 `开发任务.md`，**尚未拆 spec**
- 代码层无落地，`src/pages/` 下无画布相关页面

**启动前要做的事**（按 MS2 模式）：
1. 把 `开发任务.md` 的 13 条任务拆为 spec-20 ~ spec-2x（参考 MS2 拆 5 个 spec 的力度）
2. 在 `dependencies.md` 列出 `@xyflow/react` / `zundo` 等新增前端依赖与版本
3. 先实现 React Flow 基础画布 + 一种业务节点（建议 ScriptNode 或 TaskNode），打通持久化最小闭环再扩

---

## MS4：单 Provider 多模型 + 批量 + 版本管理

- 目录：`docs/iterations/ms4-批量生成与多模型/`
- **已拆 spec-25 ~ spec-31** + `dependencies.md` + `验证清单.md`（2026-06-02）
- 范围：**百炼单 Provider** 多模型对比；即梦/第二 Provider 不在本里程碑

| spec | 主题 | 状态 | 落点 |
|---|---|---|---|
| spec-25 | 百炼多模型 capabilities + 校验 + MultiModelSelect | ✅ | `crates/core/src/provider/bailian/validate.rs`、`src/components/generation/MultiModelSelect.tsx` |
| spec-26 | 批量提交（batch_id, N×M）| ✅ | migration 009、`task_engine::submit_batch`、`BatchSubmitDialog.tsx`、batch chip |
| spec-27 | 可配置并发 + 自动重试 | ✅ | migration 011、`app_preference`、`mark_for_retry`、`set_max_concurrency` 实时切换 |
| spec-28 | 抽卡对比 + adopt | ⛔ | migration 010 `shot.adopted_asset_id`；复用 ListFilter |
| spec-29 | EpisodeCheckpoint 全量实现 | ⛔ | |
| spec-30 | 版本历史面板 UI | ⛔ | |
| spec-31 | CLI checkpoint | ⛔ | |

**spec-27 已知偏差**（待跟进 PR）：
- `error_message` 未追加 `[auto-retry n/3]` 标记（P2，仅诊断信息缺失）
- poll-stage transient 错误现在会消耗重试预算 —— 长时视频任务 ≥3 次抖动会被误杀，等真机量大后看是否要加滑窗（P2，详见 spec-27 §3.4）
- ~~测试缺 Unknown+5xx / 重试耗尽 / 实时切换并发 三条用例~~ ✅ 已补齐（280 全绿）

**spec-26 已知偏差**：
- CLI `submit-batch` 未做（spec 标注可选，后续 PR 补）
- 批量前 auto-checkpoint hook 留位（依赖 spec-29 合入后再加）

## MS5 / MS6

- 各迭代目录仍主要为 `开发任务.md` 级规划，未拆 spec

---

## 数据库 migration 总览

| # | 文件 | 引入 spec / 增量 |
|---|---|---|
| 001 | `001_initial.sql` | MS0 spec-03 全量 schema |
| 002 | `002_add_project_root_path.sql` | MS1 spec-10 |
| 003 | `003_add_api_account_key_last4.sql` | MS1 spec-13 |
| 004 | `004_add_api_account_params_json.sql` | MS2 spec-16（OSS 配置） |
| 005 | `005_add_generation_task_project_id.sql` | MS2 增量（清理流程改进） |
| 006 | `006_add_generation_task_event.sql` | MS2 增量（诊断事件流） |
| 007 | `007_add_api_account_deleted_at.sql` | MS2 增量（账号软删除） |
| 008 | `008_asset_source_label.sql` | MS1 spec-12 域增量（资源库过滤） |
| 009 | `009_generation_task_batch_id.sql` | MS4 spec-26（批量提交） |
| 010 | （预留） | MS4 spec-28 `shot.result_asset_id` |
| 011 | `011_app_preference.sql` | MS4 spec-27（并发配置）。注意 010 跳号，migrator 按版本号顺序处理 |

---

## 工程质量

- `cargo test --workspace`：270+ 通过（含 task_engine_batch、task_engine_retry）
- `cargo clippy -p mango-core --all-targets -D warnings`：干净
- `pnpm test:run`：36 files / 150 tests 通过
- `pnpm typecheck`：干净
- `pnpm build`：**当前已坏**（vite 8 + rolldown 缺 esbuild 包，备忘录已记录）；CI 路径用 `pnpm typecheck`

---

## 下一步建议（按优先级）

1. **MS2 真机 smoke**：把上面「真机验证 backlog」四项跑掉，确认端到端可用后正式收口 MS2
2. **MS3 spec 拆分**：参考 MS2 拆法（5 个 spec + dependencies），把节点画布的 13 条任务拆成可独立 PR 的最小单元；先把当前分支的范围确定下来
3. **不阻塞的小尾巴**：`character.reference_asset_id` 列、账号默认值、命名整理等可以挑一两个并入 MS3 启动 PR
