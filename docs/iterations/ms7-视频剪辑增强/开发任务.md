# MS7: 视频剪辑增强

## 迭代目标

补齐漫剧成片所需的视觉编辑能力：多轨时间轴、镜头动态化（Ken Burns / 关键帧）、转场、文字系统（字幕/气泡/花字/拟声词）、贴片装饰、视觉特效。使用户在 Mango 内完成从 AI 生图到可发布漫剧视频的全部剪辑工作，无需导出到第三方工具。

**预估工时**：120–160 小时（~8–12 周，拆 8 个任务 / 首批 4 个 spec）

**前置依赖**：MS5（FFmpeg 基础命令 + 单轨时间轴）、MS6（配音/音效/字幕烧录 + 成片合成）

---

## 与 MS5/MS6 的边界

| 能力 | MS5 | MS6 | MS7（本里程碑） |
|---|---|---|---|
| 视频裁剪/拼接 | ✅ | — | 保留 |
| 进度解析 | ✅ | — | 复用 |
| 单轨时间轴 | ✅ | — | 升级为多轨 |
| AI 配音 + 音效 | — | ✅ | — |
| 字幕烧录（ASS） | — | ✅ | 扩展为完整文字系统 |
| 最终合成（简单管线） | — | ✅ | 升级为 filter graph 管线 |
| 镜头动态化 | — | — | ✅ 新增 |
| 转场 | — | — | ✅ 新增 |
| 花字/贴片/视觉特效 | — | — | ✅ 新增 |
| 多轨时间轴 | — | — | ✅ 新增 |
| 导出增强 | — | 基础 | ✅ 增强 |

---

## 📌 spec 拆分结构（2026-06-10）

任务 1-3 已拆分为 **4 个 spec + 依赖一览 + 验证清单**。**新开发请直接参照对应 spec**；下文任务清单保留为索引。

任务 4-8 待首批 spec 实现后再做评审拆分。

| Spec | 文件 | 范围 | 原任务 | 工时 |
|---|---|---|---|---|
| **42** | [spec-42-filter-graph-video.md](spec-42-filter-graph-video.md) | Filter Graph 视频扩展 + render.rs | 任务 1（完整） | 10–14h |
| **43** | [spec-43-timeline-data-model.md](spec-43-timeline-data-model.md) | 多轨时间轴 DB + 查询 + IPC + 音频导入 | 任务 3（后端） | 10–14h |
| **44** | [spec-44-multi-track-ui.md](spec-44-multi-track-ui.md) | 多轨 Store + UI 组件 + 代理预览 | 任务 3（前端） | 16–22h |
| **45** | [spec-45-render-pipeline.md](spec-45-render-pipeline.md) | 重编码管线 + export_timeline | 任务 2（完整） | 8–12h |
| **依赖** | [dependencies.md](dependencies.md) | Rust/npm/migration/command 一览、PR 顺序 | — | — |
| **验证** | [验证清单.md](验证清单.md) | 分 spec + 端到端演示勾选 | — | — |

### 建议 PR 实施顺序

见 [dependencies.md §7](dependencies.md#7-建议-pr-实施顺序)。

```
spec-42 (filter graph, 10-14h) ─────────┐
                                          ├─▸ spec-45 (render pipeline, 8-12h)
spec-43 (timeline data, 10-14h) ────────┤
    │                                    
    └─▸ spec-44 (timeline UI, 16-22h)
```

spec-42 和 spec-43 无互相依赖，可并行开发。

---

## 关键设计修正（相对原始任务书）

1. **filter_graph.rs 已存在** — 「扩展现有」而非「新建」（184 行，6 个音频 filter，被 audio.rs 依赖）
2. **删除 FilterChain 抽象** — 扁平 `Vec<FilterNode>` 模型足够，不增加不必要的层
3. **新增 normalize 方法** — xfade/concat 要求同分辨率/帧率/像素格式，现有 `check_concat_compatibility` 仅检查 Copy 模式
4. **probe.rs 扩展 pixel_format** — VideoMetadata 新增字段，供 normalize 判断
5. **保留现有导出函数** — `export_resolved_clips` / `export_final` 不动，新建 `export_timeline` 读 timeline_* 表
6. **替代 Timeline + ClipAssembly 两者** — 多轨时间轴统一 clip 列表和裁剪时间轴
7. **TimelineControls 需重写** — trimStart/trimEnd 接口不适用多轨
8. **补齐三个缺失设计** — FFmpeg 代理预览、MS6 音频导入机制（`import_audio_from_shots`）、`TimelineItemParams` typed enum

---

## 任务清单（索引）

| 原任务 | 归入 Spec | 状态 |
|--------|-----------|------|
| 任务 1: FFmpeg filter graph builder | spec-42 | — |
| 任务 2: 重编码导出管线 | spec-45 | — |
| 任务 3: 多轨时间轴（后端） | spec-43 | — |
| 任务 3: 多轨时间轴（前端） | spec-44 | — |
| 任务 4: 关键帧 + Ken Burns | 待拆分 | — |
| 任务 5: 转场库 | 待拆分 | — |
| 任务 6: 文字系统 | 待拆分 | — |
| 任务 7: 贴片/特效 | 待拆分 | — |
| 任务 8: 导出增强 | 待拆分 | — |

---

## ⚠️ 任务 4-8 待拆分

任务 4-8 保持原始设计（见 [开发任务-v1.md](开发任务-v1.md)），待 spec-42~45 实现后再做评审修正和 spec 拆分。届时需对齐：
- spec-42 建立的 `FilterGraph` 视频 filter API 表面
- spec-45 建立的 `RenderConfig` / `export_timeline()` 接口
- spec-43 建立的 `TimelineItemParams` enum 各 variant
- spec-43 建立的 `timeline_*` 表 schema 和查询 API

已知需关注的点：
- 任务 4（关键帧）：zoompan 的多段 keyframe 表达式复杂度，可能需要改用 crop+scale 方案
- 任务 5（转场）：xfade 要求输入同分辨率/格式，已由 spec-45 的 normalize 步骤解决
- 任务 6（文字系统）：范围过大，建议拆为 字幕 / 气泡 / 花字+拟声词 三个子 spec
- 任务 7（贴片+特效）：内置素材资产的来源和格式需要确定
- 任务 8（导出增强）：封面帧自动推荐的图像分析方案需要细化

---

## 内置素材资源清单

MS7 需要打包一批内置素材。首版数量可控，后续扩展。

| 类型 | 数量 | 格式 | 存储位置 |
|---|---|---|---|
| Ken Burns 预设 | 8 | JSON 参数 | 代码内置 |
| 转场类型 | 8 | FFmpeg xfade 参数 | 代码内置 |
| 花字模板 | 15-20 | JSON（样式参数）+ PNG（装饰背景） | `assets/templates/fancy_text/` |
| 拟声词模板 | 10+ | JSON + PNG | `assets/templates/onomatopoeia/` |
| 表情贴纸 | 20+ | SVG/PNG | `assets/stickers/emotion/` |
| 特效贴纸 | 10+ | PNG 序列或 WebM | `assets/stickers/effects/` |
| 粒子序列 | 6 | WebM（透明通道） | `assets/particles/` |
| 边框模板 | 5+ | SVG/PNG | `assets/templates/frames/` |
| 调色预设 | 10 | JSON（filter 参数） | 代码内置 |

---

## 迭代交付物

- FFmpeg filter graph builder + 重编码管线
- 多轨时间轴（视频/文字/贴片/音频轨）
- 关键帧动画系统 + 8 个 Ken Burns 预设
- 8 种转场效果
- 完整文字系统（字幕/气泡/花字/拟声词 + 模板库）
- 贴片/装饰系统 + 视觉特效（调色/粒子/镜头）
- 导出增强（宽高比/平台预设/封面帧/批量导出）

## 演示验收

> 打开一个包含 5 个 AI 生成画面的漫剧集 → 进入多轨时间轴 → 为每个画面选择 Ken Burns 预设（推进/平移/拉远交替使用）→ 在相邻画面之间添加叠化和翻页转场 → 添加底部字幕（角色分色）→ 为关键对白添加漫画气泡 → 插入花字标题「第一话·命运的相遇」→ 在打斗场景插入「砰！」拟声词 + 冲击线特效 + 镜头抖动 → 添加汗滴表情贴纸 → 全片应用暖调调色 → 导出为 9:16 抖音版本 → 再导出 16:9 B站版本 → 播放两个版本，画面/文字/特效/转场/音频全部正确

逐项勾选见 [验证清单.md](验证清单.md)。

---

原始完整任务清单（含任务 1-8 详细概设）保留在 [开发任务-v1.md](开发任务-v1.md) 中。
