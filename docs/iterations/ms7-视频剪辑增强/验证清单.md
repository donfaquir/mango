# MS7 验证清单

> 演示场景：打开漫剧集 → 进入多轨时间轴 → 添加片段到视频轨 → 导入 MS6 音频 → 添加转场 → 导出成片 → 播放确认。

**前置**：MS5 已完成（FFmpeg sidecar）、MS6 已完成（配音 + 音效绑定）。至少一个 episode 包含多个带音频绑定的 shot。

---

## spec-42: Filter Graph 视频扩展

- [ ] 现有 6 个音频 filter 方法 API 不变，`audio.rs` 无需修改
- [ ] `scale` filter 生成 `[in]scale=W:H[out]`
- [ ] `normalize` 生成逗号分隔单链 `scale=W:H,fps=F,format=FMT`
- [ ] `xfade` 生成正确的双输入 + transition 参数
- [ ] `drawtext` 特殊字符（冒号/引号）正确转义
- [ ] `overlay` 带 enable 表达式正确生成
- [ ] `zoompan` zoom/x/y 表达式正确嵌入
- [ ] 复杂 graph（5+ 输入、视频+音频混合）build() 无语法错误
- [ ] `RenderConfig` Default 合理（libx264, fast, crf=18, mp4）
- [ ] `cargo test -p mango-core` 新增 filter graph 测试全通过

---

## spec-43: Timeline 数据模型

- [ ] Migration 015 创建 3 个表 + 6 个索引
- [ ] `create_default_tracks` 创建 6 条默认轨道（类型/标签/顺序正确）
- [ ] Track CRUD：创建/删除/重排/mute/lock
- [ ] Item CRUD：创建/更新/移动/删除
- [ ] 删除轨道级联删除 items，删除 item 级联删除 keyframes
- [ ] `batch_create` 事务批量插入
- [ ] `TimelineItemParams` 5 个 variant 序列化/反序列化 roundtrip
- [ ] 非法 params_json 返回 ValidationError
- [ ] `import_audio_from_shots`：voice/sfx/bgm 正确映射到对应轨道
- [ ] 音频导入 position_ms 正确累加（timeline_offset + binding.offset_ms）
- [ ] 现有 `video_clip` 表和查询不受影响
- [ ] tauri-specta TS bindings 包含所有新类型
- [ ] `cargo test -p mango-core` 新增 21+ 测试全通过

---

## spec-44: 多轨 UI + Store

- [ ] 多轨编辑器替代原有 Timeline + ClipAssembly
- [ ] 空 episode 首次进入自动创建 6 条默认轨道
- [ ] 已有轨道的 episode 正确加载
- [ ] 可在轨道上添加 item
- [ ] item 水平拖拽移动（改 position_ms）
- [ ] item 跨轨道拖拽（改 track_id + position_ms）
- [ ] item 左右 handle 调整 in/out point
- [ ] 点击选中 item，shift+click 多选
- [ ] Undo/Redo 对 track/item 操作生效
- [ ] zoom/scroll/playhead 不进 undo 栈
- [ ] init/reset 不进 undo 栈
- [ ] 时间轴数据 auto-save（1s debounce）
- [ ] 播放按钮触发代理渲染 → 进度条 → 播放预览
- [ ] 编辑后 proxy 标记为 stale
- [ ] TrackHeader mute/lock 按钮工作
- [ ] 100+ item 滚动流畅
- [ ] `pnpm typecheck` 零错误

---

## spec-45: Render Pipeline + Export

- [ ] `VideoMetadata.pixel_format` 对 mp4/webm 正确返回
- [ ] 纯 clip 无特效 → 自动 Copy 模式
- [ ] 有 transition/特效 → Render 模式
- [ ] 不同分辨率 clip → normalize 统一后拼接
- [ ] CRF / preset / codec 参数可配置且生效
- [ ] 进度条在 Render 模式下正常工作
- [ ] 输出文件可在 VLC / QuickTime 播放
- [ ] ExportSettingsSheet 增加 timeline 导出选项
- [ ] CLI `mango export timeline` 可用
- [ ] 现有 `export_video_clips` / `export_final` 行为不变
- [ ] `cargo test -p mango-core` 新增 11+ 测试全通过

---

## 端到端演示（必须）

1. [ ] 确认 FFmpeg 可用（`check_ffmpeg` 返回 available=true）
2. [ ] 打开项目 → 进入某集 → 确认有 3+ 个带音频绑定的分镜
3. [ ] 进入视频编辑器 → 观察 6 条默认轨道自动创建
4. [ ] 在视频轨添加 3 个 clip（来自已生成视频 asset）
5. [ ] 拖拽调整 clip 顺序和位置
6. [ ] 调整某 clip 的 in/out point（左右 handle）
7. [ ] 点击"导入 MS6 音频" → 确认配音/音效/BGM 出现在对应轨道
8. [ ] 音频 item 位置与分镜时间轴对齐
9. [ ] Undo 两步 → clip 回到之前位置 → Redo 恢复
10. [ ] 点击播放 → 观察代理渲染进度 → 渲染完成后预览播放
11. [ ] 修改一个 clip 位置 → 确认 proxy 标记 stale
12. [ ] 打开导出设置 → 选择 Preset=fast / CRF=18
13. [ ] 点击"时间轴导出" → 进度 0-100%
14. [ ] 导出完成 → 播放输出视频，画面+音频正确
15. [ ] CLI `mango export timeline --episode <id> --output ./test.mp4` 产出相同结果

---

## 回归

- [ ] 现有"导出片段"（export_video_clips）功能正常
- [ ] 现有"最终导出"（export_final）功能正常
- [ ] 画布节点编辑正常
- [ ] AI 生图/生视频流程不受影响
- [ ] 角色配音生成正常
- [ ] Checkpoint 功能正常
- [ ] `pnpm typecheck` 零错误
- [ ] `cargo clippy` 零新增 warning
- [ ] `cargo test -p mango-core` 全通过
- [ ] `pnpm test:run` 全通过
