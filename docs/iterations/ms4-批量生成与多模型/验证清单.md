# MS4 验证清单

> 对应演示故事：`docs/iterations/ms4-批量生成与多模型/开发任务.md` §演示验收。  
> 按 [dependencies.md](dependencies.md) 建议顺序分 spec 勾选；全部通过视为 MS4 闭幕。

**前置**：MS2 百炼单任务可用；MS3 spec-20 + spec-24 最小合入（分镜 + 保存版本占位）。

---

## spec-25 百炼多模型

- [x] `model.capabilities_json` 对 wan27 / happyhorse 可解析
- [x] 非法 params 提交前失败，无脏任务行
- [x] ModelPicker / MultiModelSelect 按 task_type 过滤

## spec-26 批量提交

- [x] migration 009 已应用
- [x] 8 分镜 × 2 模型 → 16 任务，同一 `batch_id`
- [x] 确认对话框展示任务数正确
- [x] 任务面板可按 batch 筛选
- 跟进项：CLI `submit-batch` 未做（spec 标可选）；批量前 auto-checkpoint hook 留位（依赖 spec-29）

## spec-27 并发 + 重试

- [x] migration 011；默认并发 3
- [x] 设置页修改并发后新任务遵守上限（实时生效）
- [x] stub/模拟限流错误自动重试 ≤3 次
- [x] Auth/Quota 不重试
- [x] Unknown + 5xx 走重试路径（`unknown_5xx_errors_are_retried`）
- [x] 重试耗尽后 retry_count==3 且 final failed（`retry_exhaustion_caps_at_max_then_fails`）
- ⚠️ **行为变更**：poll-stage transient 错误现在也消耗重试预算，长时任务（10 分钟视频）若 ≥3 次零星抖动会被误杀，详见 spec-27 §3.4

## spec-28 抽卡对比

- [ ] migration 010（`shot.adopted_asset_id`）
- [ ] `list_tasks(shot_id=X, status=success)` 返回正确结果
- [ ] 某分镜 2+ 条 success 任务在 GachaCompareDialog 并排显示
- [ ] 「采用」后 `shot.adopted_asset_id` 正确；切换采用覆盖旧值
- [ ] 「取消采用」后 `adopted_asset_id` 为 NULL
- [ ] ShotCard 上显示已采用结果缩略图
- [ ] 预览放大/视频播放正常（复用 ResultPreviewDialog）

## spec-29 Checkpoint 核心

- [ ] 手动创建保存 script + shots（含 adopted_asset_id）+ canvas
- [ ] 无 canvas 时创建不报错（兜底空 JSON）
- [ ] change_summary 非空且可读（含分镜新增/修改/删除数量）
- [ ] 恢复后 shots、canvas、script 与快照一致
- [ ] 恢复前自动创建 auto 检查点
- [ ] 30min 定时有变更 → auto 行；无变更不产生
- [ ] 批量提交前 auto 触发
- [ ] GC 不删 manual；auto 按年龄规则压缩
- [ ] list 返回轻量列表项（不含大 JSON）

## spec-30 版本面板

- [ ] `useCheckpointAutoSave` 和 `useEpisodeDataRestoredListener` 已挂载到 CanvasInner
- [ ] 「保存版本」弹出 SaveVersionDialog，可输入标签（留空→NULL）
- [ ] 「版本历史」弹出 VersionHistoryDialog，列表正确展示
- [ ] manual / auto 视觉可区分（图标 + 标题格式不同）
- [ ] 恢复有二次确认，文案包含数据丢失风险提示
- [ ] 恢复后画布、分镜、剧本数据自动刷新
- [ ] 删除 manual checkpoint 需确认

## spec-31 CLI

- [ ] `mango checkpoint create` 行在 GUI 可见
- [ ] `mango checkpoint restore --yes` 与 GUI 恢复等价

---

## 端到端演示（必须）

1. [ ] 打开第三集，选中 8 个分镜
2. [ ] 百炼下选 2 个模型批量提交 → 16 任务完成
3. [ ] 在抽卡对比中逐分镜采用最佳结果
4. [ ] 「保存版本」标签「分镜定稿」
5. [ ] 故意改坏分镜/画布
6. [ ] 从版本历史恢复到「分镜定稿」，数据回滚正确

---

## 回归

- [x] MS2 单条 `submit_task` 仍正常，`batch_id` 为 NULL（test verified）
- [ ] spec-24「保存版本」不报错（升级为 full 快照后） —— spec-29 时验证
- [x] `cargo test` workspace 全绿（270+ 通过）
- [x] 前端测试全绿（36 文件 / 150 测试）
