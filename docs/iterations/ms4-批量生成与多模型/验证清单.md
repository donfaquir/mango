# MS4 验证清单

> 对应演示故事：`docs/iterations/ms4-批量生成与多模型/开发任务.md` §演示验收。  
> 按 [dependencies.md](dependencies.md) 建议顺序分 spec 勾选；全部通过视为 MS4 闭幕。

**前置**：MS2 百炼单任务可用；MS3 spec-20 + spec-24 最小合入（分镜 + 保存版本占位）。

---

## spec-25 百炼多模型

- [ ] `model.capabilities_json` 对 wan27 / happyhorse 可解析
- [ ] 非法 params 提交前失败，无脏任务行
- [ ] ModelPicker / MultiModelSelect 按 task_type 过滤

## spec-26 批量提交

- [ ] migration 009 已应用
- [ ] 8 分镜 × 2 模型 → 16 任务，同一 `batch_id`
- [ ] 确认对话框展示任务数正确
- [ ] 任务面板可按 batch 筛选

## spec-27 并发 + 重试

- [ ] migration 011；默认并发 3
- [ ] 设置页修改并发后新任务遵守上限
- [ ] stub/模拟限流错误自动重试 ≤3 次

## spec-28 抽卡对比

- [ ] migration 010
- [ ] 某分镜 2+ 条 success 任务并排显示
- [ ] 「采用为结果」后 `shot.result_asset_id` + `status=done`
- [ ] 预览放大/视频播放正常

## spec-29 Checkpoint 核心

- [ ] 手动保存含 shots + canvas + change_summary
- [ ] 恢复前后数据一致；恢复前多一条 auto 检查点
- [ ] 批量提交前自动检查点（若已接 hook）
- [ ] 启动后 GC 不删除 manual

## spec-30 版本面板

- [ ] 版本历史列表 manual/auto 可区分
- [ ] 恢复二次确认
- [ ] 标签可编辑（manual）

## spec-31 CLI

- [ ] `mango checkpoint create` 行在 GUI 可见
- [ ] `mango checkpoint restore --yes` 与 GUI 恢复等价

---

## 端到端演示（必须）

1. [ ] 打开第三集，选中 8 个分镜
2. [ ] 百炼下选 2 个模型批量提交 → 16 任务完成
3. [ ] 在抽卡对比中逐分镜采用最佳结果
4. [ ] 「保存版本」标签「分镜定稿」
5. [ ] 故意改坏分镜/画布
6. [ ] 从版本历史恢复到「分镜定稿」，数据回滚正确

---

## 回归

- [ ] MS2 单条 `submit_task` 仍正常，`batch_id` 为 NULL
- [ ] spec-24「保存版本」不报错（升级为 full 快照后）
- [ ] `cargo test` workspace 全绿
- [ ] 前端测试全绿
