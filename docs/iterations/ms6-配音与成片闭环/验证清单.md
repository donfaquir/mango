# MS6 验证清单

> 演示场景：确认百炼账号已配置 → 为角色选择音色 → 批量生成配音 → 导入音效绑定到分镜 → 导出带配音+音效的成片。

**前置**：MS5 已完成（FFmpeg sidecar 可用、视频导出 pipeline 正常），至少一个 episode 包含多个有 dialogue 的 shot。

---

## spec-37: TTS 接入（百炼 CosyVoice）

- [ ] `list_models(provider_id="bailian")` 返回 cosyvoice-v2 模型信息
- [ ] `submit_task` 使用 provider_id="bailian", model_id="cosyvoice-v2" 可提交 TTS 任务
- [ ] 任务完成后 result_asset_id 指向有效的 .mp3 文件
- [ ] 复用现有百炼账号（无需额外配置）
- [ ] API key 无效时返回明确的 Auth 错误
- [ ] `cargo test -p mango-core` 新增 TTS 相关测试全通过

---

## spec-38: 角色配音生成

- [ ] 角色编辑页显示"配音音色"选择区域
- [ ] 可选择预设音色并保存到角色
- [ ] 试听按钮播放该音色的示例语音
- [ ] 更换音色后保存，character.voice_id 更新
- [ ] 单分镜"生成配音"按钮 → 生成对应音频 asset
- [ ] "为全集生成配音" → 批量提交，跳过无对白/无音色的 shot
- [ ] 生成进度在 UI 实时显示
- [ ] 生成结果：audio asset 关联到 shot，label="voice"
- [ ] 无对白的 shot 不会出现在生成列表中

---

## spec-39: 音效管理与分镜绑定

- [ ] 可导入 .mp3/.wav/.ogg/.m4a 音频文件到项目
- [ ] 导入的音频 asset 出现在资源列表中
- [ ] Shot 详情面板显示音频绑定区域（按角色分组）
- [ ] 可为 shot 添加音效绑定（选择已有音频 asset）
- [ ] 音量滑块调整生效（0-200%）
- [ ] 偏移量输入生效
- [ ] 可删除绑定（asset 本身不受影响）
- [ ] 配音生成完成后自动出现在"配音"分组
- [ ] voice/bgm 唯一性约束生效（不能绑定 2 个 voice）
- [ ] 删除 shot 时其音频绑定一并清除

---

## spec-40: FFmpeg 音频处理基础设施

- [ ] `probe_audio_duration` 对 mp3/wav 返回正确毫秒时长
- [ ] `check_audio_alignment` 正确判断 Exact/PadSilence/TrimAudio/RegenerateNeeded
- [ ] AlignmentBadge 在 shot 详情中显示正确状态颜色
- [ ] FilterGraph builder 单元测试全通过（6+ 用例）
- [ ] `mix_audio` 集成测试：2 文件混合输出正确
- [ ] `overlay_audio_on_video` 集成测试：输出有视频+音频流
- [ ] `pad_audio_to_duration` 集成测试：时长匹配
- [ ] `trim_audio` 集成测试：时长匹配

---

## spec-41: 最终合成与导出

- [ ] ExportSettingsSheet 显示分辨率/码率/格式/音频轨选项
- [ ] 预估文件大小随设置变化更新
- [ ] 导出进度 0-100% 在 overlay 中显示
- [ ] 导出完成：弹出完成对话框（打开文件/打开文件夹）
- [ ] 输出 MP4 包含视频+混合音频
- [ ] 音频轨的音量/偏移在输出中正确体现
- [ ] 关闭"配音"开关 → 输出无配音轨
- [ ] 关闭"音效"开关 → 输出无音效
- [ ] 源分辨率导出 → 视频不重编码（快）
- [ ] 720p 导出 → 输出分辨率正确
- [ ] CLI `mango export final --episode <id> --output ./out.mp4` 正常工作
- [ ] CLI 输出与 GUI 结果一致

---

## 端到端演示（必须）

1. [ ] 确认已配置百炼账号（复用现有，无需额外操作）
2. [ ] 打开项目 → 进入某集 → 确认有 3+ 个带对白的分镜
3. [ ] 编辑角色"小明" → 选择少年音色 → 试听确认 → 保存
4. [ ] 点击"为全集生成配音" → 观察进度 → 全部完成
5. [ ] 确认每个有对白分镜下出现了配音绑定
6. [ ] 导入 2 个音效文件（打斗声、脚步声）
7. [ ] 为打斗分镜绑定打斗音效（音量 80%，偏移 200ms）
8. [ ] 为打斗分镜绑定脚步音效（音量 60%，偏移 0ms）
9. [ ] 确认 AlignmentBadge 显示绿色（Exact）或黄色（可自动处理）
10. [ ] 打开导出设置 → 选择 1080p / MP4 / 全部音轨
11. [ ] 确认预估大小合理
12. [ ] 点击"开始导出" → 进度 0-100%
13. [ ] 导出完成 → 点击"打开文件" → 播放视频
14. [ ] 验证：画面正确 + 配音同步 + 音效在正确时间点播放 + 音量合理
15. [ ] 在终端执行 `mango export final --episode <id> --output ./test.mp4`
16. [ ] 确认 CLI 输出与 GUI 一致

---

## 回归

- [ ] 现有"导出片段"（v1 纯视频）功能不受影响
- [ ] 角色 CRUD 正常（不影响无 voice_id 的场景）
- [ ] 画布节点编辑正常
- [ ] AI 生图/生视频流程不受影响
- [ ] checkpoint 功能正常
- [ ] `pnpm typecheck` 零错误
- [ ] `cargo clippy` 零新增 warning
- [ ] `cargo test -p mango-core` 全通过
